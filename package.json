{
  "name": "infinity-earth",
  "version": "1.0.0",
  "description": "Plataforma colaborativa de apuntes científicos",
  "main": "index.html",
  "scripts": {
    "build:android": "npx cap sync android && npx cap build android",
    "open:android": "npx cap open android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
